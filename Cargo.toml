[package]
name = "treecode"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2"

[dev-dependencies]
assert_cmd = "2.0"
approx = "0.5"
tempfile = "3.0"
libc = "0.2"

[[bench]]
name = "throughput"
harness = false

[profile.release]
debug = true
lto = "thin"
codegen-units = 1
